{
  "version": 1,
  "hooks": {
    "beforeSubmitPrompt": [
      {
        "command": "/usr/local/bin/bash -c \"cd ${CURSOR_PROJECT_DIR} && ~/.cursor/hooks/on_submit.sh\"",
        "timeout": 8
      }
    ],
    "afterAgentResponse": [
      {
        "command": "/usr/local/bin/bash -c \"cd ${CURSOR_PROJECT_DIR} && ~/.cursor/hooks/on_response.sh\"",
        "timeout": 8
      }
    ],
    "beforeShellExecution": [],
    "sessionEnd": []
  }
}
